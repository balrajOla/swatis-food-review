import Image from 'next/image'; import Link from 'next/link'; import type { Review } from '@/lib/types';
export function ReviewCard({ review, priority=false }: { review: Review; priority?: boolean }) { return <article className="group reveal hover-lift"><Link href={`/reviews/${review.slug}`} className="block overflow-hidden rounded-[2rem] bg-white shadow-soft"><div className="relative aspect-[4/3] overflow-hidden"><Image src={review.image} alt={review.title} fill priority={priority} className="object-cover transition duration-700 group-hover:scale-105"/></div><div className="p-7"><div className="mb-4 flex items-center justify-between text-[10px] uppercase tracking-[.25em] text-clay"><span>{review.category}</span><span>{review.rating.toFixed(1)} / 5</span></div><h3 className="font-serif text-3xl leading-tight">{review.title}</h3><p className="mt-3 text-sm leading-7 text-ink/60">{review.dek}</p><p className="mt-5 text-xs uppercase tracking-[.22em] text-ink/45">{review.venue} · {review.location}</p></div></Link></article> }
