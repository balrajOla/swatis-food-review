import { getReviews } from '@/lib/reviews'; import { ReviewCard } from '@/components/ReviewCard';
export default async function Reviews(){ const reviews=await getReviews(); return <main className="mx-auto max-w-7xl px-5 py-16"><h1 className="font-serif text-6xl md:text-8xl">All reviews</h1><p className="mt-5 max-w-2xl leading-8 text-ink/60">Browse Swati’s food notes by venue, place and mood. Filters/search can be added once the content library grows.</p><div className="mt-12 grid gap-8 sm:grid-cols-2 lg:grid-cols-3">{reviews.map(r=><ReviewCard key={r.id} review={r}/>)}</div></main> }
